## 一、代码规范

### 1.1 Alembic 迁移脚本

创建或修改 Alembic 迁移脚本时，**必须**为每个表和每个字段添加注释，便于后续数据库审计和团队协作。

**要求：**

- 表注释：使用 `comment='...'` 参数，说明该表的业务用途
- 字段注释：每个 `sa.Column` 都需 `comment='...'`,说明字段含义、单位、枚举值等
- 注释语言：统一使用中文（与项目其他文档保持一致）
- 注释内容：描述"是什么"和"为什么"，而非重复字段名