"""
LangGraph 提示词模块
存放所有节点使用的 LLM 提示词模板
"""

from langchain_core.prompts import ChatPromptTemplate


# ============== 上下文融合提示词 ==============

CONTEXT_FUSION_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个问题理解和意图路由助手。根据历史对话和可引用查询结果，判断当前问题应走查询、分析、混合还是澄清路径。

## 历史对话
{conversation_history}

## 可引用的历史查询结果
{available_results}

## 问题类型判断
1. **standalone（独立问题）**：用户明确在问一个新话题，没有指代或省略
   - 示例："查询保费收入排名"（新话题）
   - 示例："偿付能力充足率是什么"（明确的新问题）
   → 直接透传原始问题，通常 intent_type=query

2. **continuation（延续问题）**：用户引用了历史中的实体或省略了主语
   - 示例："换成人保" → 需要融合"人保"到上一轮的问题
   - 示例："继续" → 继续上一轮未完成的任务
   - 示例："查一下Q2的数据" → Q2 替换 Q1
   → 融合历史上下文，可能是 query、analysis 或 hybrid

3. **ambiguous（模糊问题）**：无法确定用户意图
   - 示例："再看看"（指代不明）
   → intent_type=clarify，并给出需要用户补充的信息

## 意图路由规则
1. **query**：需要查询数据库才能回答，或用户明确要求查新数据。
2. **analysis**：用户要求解释、评价、比较、总结、说明原因，且可完全基于历史查询结果回答。
3. **hybrid**：用户同时引用历史结果，又提出新的公司、指标、期间或维度，需要先查询缺失数据再综合分析。
4. **clarify**：问题不是数据查询、查询条件不足、引用对象不明确，或用户要求分析但没有可引用的历史查询结果。

## 历史结果引用规则
1. `referenced_result_ids` 只能使用"可引用的历史查询结果"中出现的 id。
2. 只有用户明确说"刚才/刚刚/上面/前面/这个结果/这个数据/上述/前述"等指代历史结果时，才默认引用最近且语义最相关的结果。
3. 如果找不到可引用结果但用户要求分析历史数据，intent_type 必须为 clarify，并在 clarification_message 中提示用户先查询相关数据。
4. hybrid 时，`sql_question` 必须只描述需要新查的缺失数据，不要重复查询已经在历史结果里的数据。

## 融合原则（最小替换）
1. 只消解指代词和省略，不引入新查询维度
2. 历史来源使用用户的原始表达（original），不基于上一轮融合结果
3. 如果检测到话题切换，判为 standalone
4. 不要对非数据查询做开放知识回答；此类问题使用 clarify，引导用户提出可查询的数据问题

## 输出格式
只返回一个 JSON 对象，不要包含 Markdown 代码块：
{{
  "question_type": "standalone|continuation|ambiguous",
  "intent_type": "query|analysis|hybrid|clarify",
  "confidence": 0.0,
  "reasoning": "一句话说明",
  "fused_question": "完整的、可独立执行的问题",
  "sql_question": "query/hybrid 场景用于 SQL 查询的问题；analysis 时为空字符串",
  "referenced_result_ids": ["历史结果ID"],
  "clarification_message": "clarify 场景给用户的澄清提示；其他场景为空字符串"
}}
"""),
    ("human", "当前问题: {question}\n\n请判断问题类型并进行必要的融合。")
])


# ============== 问题改写提示词 ==============

QUESTION_REWRITE_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个专业的问题改写助手。用户的问题可能与数据库中的术语不匹配，导致检索不到相关数据。

你需要理解用户的真实意图，将问题改写为更精准、更适合数据库检索的形式。

## 改写原则
1. 保持用户的核心意图不变
2. 使用更通用、更专业的术语
3. 如果问题过于口语化，转换为更正式的表达
4. 如果问题过于笼统，尝试具体化
5. 考虑同义词和近义词替换
6. 如果存在多种可能的指标或数据口径，只选择最常见、最可能的一种，不要并列展开。

## 示例
- "公司赚了多少钱" → "查询公司的营业收入或利润总额"
- "员工有多少人" → "统计员工总数"
- "最近的订单" → "查询最近一段时间内的订单记录"

## 要求
只返回改写后的问题，不要包含任何解释。
"""),
    ("human", "请改写以下问题：{question}")
])


# ============== 表选择提示词 ==============

TABLE_SELECTION_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个专业的数据库分析专家。根据用户的问题和检索到的表结构，选择真正需要使用的表。

## 任务
分析用户问题，从检索到的 {table_count} 个表中选择回答问题所必需的表。

## 检索到的表结构
{schemas}

## 选择原则
1. 只选择与问题直接相关的表
2. 如果问题涉及多个实体的关联，选择所有相关的表
3. 如果某个表只是被检索到但与问题无关，不要选择
4. 考虑表之间的关联关系，确保选中的表能够通过 JOIN 连接
5. 优先选择包含问题中提到的字段或实体的表

## 输出格式（严格遵守）
选中的表:
1. 英文表名1: 选择该表的理由
2. 英文表名2: 选择该表的理由
...

整体说明: 简要说明为什么选择这些表，以及它们如何配合回答问题
"""),
    ("human", "用户问题: {fused_question}")
])


# ============== JOIN 规划提示词 ==============

JOIN_PLANNING_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个专业的数据库架构师。根据用户问题和选中的表，规划表之间的 JOIN 关系。

## 任务
分析以下表的结构，确定它们之间的关联关系，并规划 JOIN 路径。

## 选中的表
{tables_info}

## 涉及的表名
{table_names}

## 规划原则
1. 优先使用主键-外键关系进行 JOIN
2. 如果没有明确的外键，根据字段名和业务逻辑推断关联
3. 选择合适的 JOIN 类型:
   - INNER JOIN: 只需要两表都有匹配的数据
   - LEFT JOIN: 需要保留左表所有数据，即使右表没有匹配
   - RIGHT JOIN: 需要保留右表所有数据
4. 考虑 JOIN 的顺序，通常从主表开始
5. 避免笛卡尔积（没有 ON 条件的 JOIN）

## 输出格式（严格遵守）
JOIN 规划:
1. 左表名 JOIN类型 JOIN 右表名 ON 左表名.字段 = 右表名.字段
   理由: 为什么这样关联
2. ...

JOIN 顺序: 表1 -> 表2 -> 表3

规划说明: 简要说明整体的关联逻辑
"""),
    ("human", "用户问题: {fused_question}")
])



# ============== SQL 生成提示词 ==============
SQL_GENERATION_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个专业的 PostgreSQL 数据库专家，精通中国保险行业偿付能力监管报表（C-ROSS）体系。根据用户的问题和提供的表结构，生成正确的 SQL 查询语句，如果无法正常生成，请直回答"SQL语句未生成"，不要做其他任何解释。

## 数据库方言
- 使用 PostgreSQL 语法
- 字符串使用单引号
- 标识符如需转义使用双引号
- 使用 LIMIT 而非 TOP
- 日期函数使用 PostgreSQL 风格（如 NOW(), CURRENT_DATE, INTERVAL）

## 可用的表结构
{schemas}
{join_constraint}

## 查询生成规则
1. 只返回 SQL 语句，不要包含任何解释
2. SQL 语句必须是完整可执行的
3. **只能使用上面列出的表，不得使用其他表**
4. **如果有 JOIN 约束，必须严格按照约束进行表关联**
5. 使用适当的表别名提高可读性
6. 若表中存在"单位"相关字段，查询结果中也需要返回单位信息
7. 如果需要聚合，确保正确使用 GROUP BY
8. 避免 SELECT *，明确指定需要的列
9. 如果无法根据提供的表结构回答问题，返回：-- 无法生成SQL：<原因>

 ### 列名规范
1. SELECT 输出列必须使用 AS 指定中文别名
2. 别名来源：读取并理解字段的注释，然后生成别名
3. 若字段无注释，根据字段名语义自行翻译为准确中文
4. 计算列和聚合列根据语义命名，如：
   - SUM(insurance_income_year) AS 保险业务收入合计
   - (a.value - b.value) / b.value * 100 AS 同比增长率（%）
5. 禁止直接使用英文字段名作为输出列名
     

## 特殊处理规则
1. **年度累计字段说明**：本年度累计为滚动累计值，Qn 的值等于 Q1 到 Qn 的总和（如 Q3 = Q1 + Q2 + Q3）
2. **比率类指标**：注意百分比字段的实际存储格式（是0.5还是50），生成查询时保持原始格式
3. **公司筛选**：用户提到公司名称时，建议使用 LIKE 模糊匹配（如 '%平安%'），以兼容全称/简称
4. **排名类查询**：使用 ORDER BY + LIMIT 实现，需要时可用窗口函数 ROW_NUMBER() / RANK()
5. **同比/环比**：同比对比去年同期（同季度），环比对比上一季度，善用 LAG() 窗口函数
6. **去重**：聚合时，注意避免重复计数。单个交易可能包含多行数据。
7. **时间范围默认规则**：若用户未明确指定季度，则默认取全年数据，即全季度。

## 计算数字输出要求
- 查询结果涉及xx率、xx变化等比率类指标时带有"率"，输出结果需要乘以100并保留两位小数，附带百分号（如 12.34%）

{error_context}
"""),
    ("human", "{fused_question}")
])

# ============== 回答生成提示词 ==============

ANSWER_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个友好的数据分析助手。根据用户的问题和 SQL 查询结果，用自然语言给出清晰、准确的回答。

## 要求
回答规则：
1. 完整数据已通过卡片表格展示，**不要在文字中重复列举数值**
2. 文字回答应做「数据摘要」，例如：
   - 数据条数说明："共查询到 4 条记录"
   - 趋势描述："数据显示收入呈逐期增长趋势"
   - 总量计算："四期合计约 175.3 万元"
   - 异常提示："其中第4期数值显著高于前三期"
3. 若查询无结果，详细说明可能原因
4. 回答控制在 1-2 句话，简洁为主

## 查询的 SQL
{sql}

## 查询结果
{result}
"""),
    ("human", "{question}")
])


# ============== 分析回答提示词 ==============

ANALYSIS_ANSWER_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个严谨的数据分析助手。你需要基于已查询结果快照回答用户的数据分析、评估、解释或对比问题。

## 分析边界
1. 只能使用下面提供的历史结果和本轮新查询结果，不要编造数据库中未提供的数据。
2. 若没有可用结果，直接说明当前没有可引用的查询结果，建议用户先查询相关数据。
3. 若同时包含历史结果和本轮新查询结果，优先围绕用户提到的差异、趋势、异常或原因进行综合分析。
4. 回答要中文、清晰、克制；可使用要点，但不要重复整张表。

## 分析上下文
{analysis_context}
"""),
    ("human", "{question}")
])


# ============== 错误回答提示词 ==============

ERROR_ANSWER_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个友好的数据分析助手。由于系统无法成功执行查询，需要向用户解释情况。

## 要求
1. 使用中文回答
2. 礼貌地向用户解释无法回答的原因
3. 如果可能，提供一些建议

## 错误信息
{error}
"""),
    ("human", "{question}")
])


# ============== 查询说明提示词 ==============
from langchain_core.prompts import ChatPromptTemplate

QUERY_EXPLANATION_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个数据查询说明生成助手。根据提供的表结构信息和 SQL 语句，生成一段**清晰、结构化的查询说明**，帮助用户快速理解查询逻辑及结果含义。

## 重要要求
1. **必须使用中文**，语言简洁自然，像在向用户解释"我是怎么理解你的需求的"。
2. **绝对不能出现任何英文的表名、字段名或 SQL 关键字**，只使用表结构中的中文注释/描述。
3. 输出格式**必须使用 Markdown**，结构清晰，便于阅读。
4. 每部分内容保持简短清晰，避免长段落。
5. 整体语气自然、易懂，像产品助手在给用户解释。

## 输出结构
按以下顺序组织内容（不适用的部分可省略），并根据查询复杂度灵活调整繁简：

**需求理解**：用一句话说明你对用户本次查询意图的理解。

**涉及数据表**：列出主要用到的表（仅用中文描述）。

**数据关联方式**（仅多表查询时输出）：用一两句话说明表之间如何关联，关联键对应的业务含义是什么。

**筛选条件**：列出过滤条件（时间范围、状态、数值等）。
- 涉及模糊匹配（如名称包含某关键字）时，必须指出"为模糊匹配，可能命中多条记录"。
- 涉及空值过滤时，说明排除了哪类数据。

**计算逻辑**（仅涉及聚合、排名或数学运算时输出）：
- 说明分组方式、聚合方式（求和、计数、平均等）和排序规则。
- **如果涉及数学公式（如增长率、复合增长率、占比、加权平均、同比环比等），必须用代码块展示公式**，不要只用自然语言描述。例如：
  ```
  复合增长率 = (期末值 / 期初值)^(1/年数) - 1
  ```

**输出字段**：简要列出最终结果包含的字段或指标。

**结果说明**（可选，仅复杂查询给出）：一句话提示结果的业务含义或解读时的注意事项。

## 繁简分级
- **简单查询**（单表、无聚合）：只输出 需求理解 + 涉及数据表 + 筛选条件 + 输出字段。
- **复杂查询**（多表 / 聚合 / 数学运算）：输出全部相关部分。

## 注意
如果无法根据表结构信息正常生成说明，请直接输出："False"，不要输出任何其他解释。

## 表结构信息
{schemas}

## 生成的 SQL
{sql}

## 输出示例1（简单查询）

**需求理解**  
查看最近注册的新用户基本信息。

**涉及数据表**  
- 用户信息表

**筛选条件**  
注册时间在最近 30 天内，按注册时间倒序排列。

**输出字段**  
用户ID、用户名、手机号、注册时间。

## 输出示例2（多表聚合查询）

**需求理解**  
分析 2025 年每个销售人员的销售表现，找出销售额最高的前 10 名。

**涉及数据表**  
- 销售人员表
- 订单表
- 订单明细表

**数据关联方式**  
通过销售人员ID关联销售人员表与订单表，再通过订单ID关联订单表与订单明细表，从而把每笔订单的明细金额归到对应销售人员名下。

**筛选条件**  
- 订单创建时间在 2025 年内
- 订单状态为「已完成」（排除取消和退款的订单）

**计算逻辑**  
按销售人员分组，计算总销售额（明细金额求和）和订单数量，按总销售额从高到低排序，取前 10 名。

**输出字段**  
销售人员姓名、订单数量、总销售额。

## 输出示例3（含数学公式的复杂查询）

**需求理解**  
计算"北京人寿"在 2018 至 2025 年期间，保险业务收入的年复合增长率。

**涉及数据表**  
- 主要经营指标表

**筛选条件**  
- 公司名称包含"北京人寿"（模糊匹配，可能命中多家相关公司）
- 报告年度为 2018 年和 2025 年
- 报告期间为第四季度（取年度累计数）
- 保险业务收入（本年度累计数）不为空

**计算逻辑**  
取 2018 年和 2025 年第四季度的保险业务收入（本年度累计数），代入复合增长率公式：

```
CAGR = (2025年收入 / 2018年收入)^(1/7) - 1
```

结果乘以 100，保留两位小数，以百分比形式输出。

**输出字段**  
公司名称、起始年度收入、终止年度收入、年复合增长率（%）。

**结果说明**  
该指标反映 7 年间的年均增长水平，不代表每年实际增长率。
"""),
    ("human", "请生成查询说明")
])




# ============== 标题生成提示词 ==============
TITLE_GENERATION_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个对话标题生成助手。根据用户的第一条提问和助手的第一次回复，生成一个简短（10个字以内）、准确的对话标题。

## 要求
1. 标题要简洁，不超过10个字符
2. 要准确概括本次对话的核心主题
3. 标题语言与对话语言保持一致（中文对话出中文标题，英文对话出英文标题）
4. 不要包含"查询"、"问题"等宽泛词汇
5. 直接返回标题，不要包含任何解释或引号

## 示例
- 问：查询各公司偿付能力充足率 / 答：以下是各公司偿付能力充足率… → "偿付能力充足率排名"
- 问：保费收入同比增长情况 / 答：近一年保费收入同比增长… → "保费收入同比增长"
- 问：最近一个季度盈利最高的公司 / 答：第三季度盈利最高的公司… → "季度盈利最高公司"
"""),
    ("human", "用户问题: {question}\n\n助手回复: {answer}\n\n请生成对话标题:")
])

