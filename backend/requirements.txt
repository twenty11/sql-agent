# LangChain & LangGraph
langchain>=0.2.0
langgraph>=0.1.0
langchain-openai>=0.1.0
langchain-huggingface>=0.0.3
langchain-community>=0.2.0

# Vector Database
pymilvus>=2.4.0

# Database — 同步引擎（LangGraph 节点使用）
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Database — 异步引擎（FastAPI 路由使用）
asyncpg>=0.29.0

# Database 迁移
alembic>=1.13.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Embeddings
# torch is installed separately in Dockerfile from the CPU-only PyTorch index.
sentence-transformers>=2.2.0

# Utilities
sqlparse>=0.5.0
sqlglot>=25.0.0
pandas>=2.2.0
openpyxl>=3.1.0
xlrd>=2.0.0

# Web Server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
gunicorn>=22.0.0
python-multipart>=0.0.9

# 认证
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Redis（异步客户端）
redis>=5.0.0

# Celery 异步任务
celery>=5.3.0

# 结构化日志（备用，原有 workflow_logger 可迁移）
loguru>=0.7.0

# 指标导出（可选）
prometheus-client>=0.20.0

# Email 验证（Pydantic）
email-validator>=2.1.0

# Tests
pytest>=8.0.0
